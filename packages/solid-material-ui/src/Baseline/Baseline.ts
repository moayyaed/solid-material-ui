import { JSX } from "solid-js";

import "./BaselineStyle";

// Empty component to import the global BaselineStyle sheet.
// TOOD: investigate better way of doing this
export function Baseline(
): JSX.Element {
    return null;
}
